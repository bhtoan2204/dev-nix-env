# Kept POSIX-compatible so Bash can source the same small alias set.
alias ll='eza --long --all --group-directories-first --git'
alias la='eza --all --group-directories-first'
alias cat='bat --paging=never'
alias lg='lazygit'
